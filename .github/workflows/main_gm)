name: Compile SAMP

on:
  push:
    branches: [ main, master ]

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download pawncc
        run: |
          wget https://github.com/pawn-lang/compiler/releases/download/v3.10.9/pawnc-3.10.9-linux.tar.gz
          tar -xzf pawnc-3.10.9-linux.tar.gz

            - name: Compile
        continue-on-error: true
        run: |
          export LD_LIBRARY_PATH=$PWD/pawnc-3.10.9-linux/lib:$LD_LIBRARY_PATH
          ulimit -s unlimited
          pawnc-3.10.9-linux/bin/pawncc -i$PWD/include gamemodes/SALRP.pwn -w0 -d0 > compile_log.txt 2>&1 || true
          echo EXIT:$? >> compile_log.txt
          ls -la gamemodes/SALRP.amx >> compile_log.txt 2>&1 || true
- name: Upload log
        uses: actions/upload-artifact@v4
        with:
          name: compile-log
          path: compile_log.txt

      - name: Upload AMX
        uses: actions/upload-artifact@v4
        with:
          name: SALRP-amx
          path: gamemodes/SALRP.amx
